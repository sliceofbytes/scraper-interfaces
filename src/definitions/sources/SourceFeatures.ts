export interface SourceFeatures {
  cases: boolean;
  deaths: boolean;
  serious: boolean;
  critical: boolean;
  recovered: boolean;
  unresolved: boolean;
  pui: boolean;
  tested: boolean;
  updateEpoch: boolean;
}
